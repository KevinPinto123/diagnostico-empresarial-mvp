# Configuración para Huawei Cloud App Engine
runtime: nodejs18

# Configuración de instancia
instance_class: F2
automatic_scaling:
  min_instances: 1
  max_instances: 10
  target_cpu_utilization: 0.6

# Variables de entorno
env_variables:
  NODE_ENV: production
  PORT: 8080

# Archivos estáticos
handlers:
  - url: /static
    static_dir: static
    secure: always
  
  - url: /.*
    script: auto
    secure: always

# Configuración de red
network:
  forwarded_ports:
    - 8080